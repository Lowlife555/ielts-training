# Claude 任务提示词包 — IELTS 训练网站 v4.0

> 使用方式：按顺序逐条执行（P0 先做，P1 后做）。每条提示词自包含，可直接粘贴给 Claude Code。
> 前置条件：项目已读取 CLAUDE.md（项目记忆自动加载）。
> 每完成一条，跑一遍现有测试/手动验证再进入下一条。

---

## P0-1：词库整理与导入（从源文件重建完整词汇库）

**任务**：以机构原始词库文件为准，整理出完整、带 List 编号、释义丰富的雅思词汇库并导入数据库。这是后续所有训练功能的数据基础，**优先做**。

**源文件位置**：`C:\Users\15782\Downloads\雅思训练词库\`（在项目目录之外；Claude Code 默认只能访问工作目录，若读取被拦截，请先确认读取该路径的权限，或启动时用 `--add-dir "C:\Users\15782\Downloads\雅思训练词库"`）

| 文件 | 结构事实（已核实） |
|------|--------------------|
| `雅思 List 1-24 英译中.pdf` | 69 页，**24 个 List，2329 词**，全局编号 1-2329 连续无缺；每条 = 全局序号 + 英文单词 + 词性；**释义列为空**（默写卷设计）；每 List 约 100 词（List 5=118、List 7=79、List 12=59、List 24=72 等不均）；List 内按字母序 |
| `雅思 List 1-24 中译英.pdf` | 24 页，24 个 List，**每 List 仅 30 个重点词，共 720 词**；每条 = 词性 + 中文释义（如"评估 v"）；英文留空；**顺序打乱**（防位置记忆）；这 30 词是从英译中同 List 抽出的重点词 |
| `PET 1-30 英译中.pdf` | 60 页，**25 个 List，约 2056 词**；每条 = 英文单词 + 词性；释义列空 |
| `PET 1-30 中译英.pdf` | 60 页，25 个 List，约 2062 词；每条 = 词性 + 中文释义；英文留空 |
| （参考）`雅思真词汇英译中.docx` / `雅思真词汇中译英.docx` | 旧版 24 表格默写卷，与新 PDF 同源，可作释义交叉参考 |

**现有数据**（勿丢弃，作为基础）：
- `server/db/seed_extracted.js` — 3955 词（IELTS 1941 + PET 2014），已导入 words 表，字段：word/part_of_speech/chinese_definition/level/topic
- 数据库 words 表**无 list_no 字段**；现有 IELTS 词与源文件有约 384 词缺口（约 125 词完全缺失，如 abstract/academic/accelerate/artificial 这类高频词）
- `server/db/ielts_list_map.json` — 辅助映射（word → {list_no, seq}，2325 个唯一词，基于英译中 PDF 全局编号）。可直接使用，也可自行解析核对

**要做**：
1. 解析 4 个源 PDF（pymupdf 或等效库；注意 PDF 文本可能是 CID hex 编码，需用带 ToUnicode 映射的解析器）。以英译中 PDF 的单词表为主表。
2. **释义来源（按优先级）**：
   - ① 中译英 PDF 同 List 匹配到的释义（30 个重点词，按"词性+语义"匹配回英译中单词）；
   - ② `seed_extracted.js` 现有 chinese_definition；
   - ③ 以上都没有的，用 DeepSeek 生成基础释义。
3. **释义扩充**：每个词给出 2-4 个核心中文义项（分号分隔，如"抽象的；纯理论的；深奥的"），不局限于文档内的单一释义。重点词（30 词/List）优先保证质量。
4. **补全缺口**：源文件中有、库里没有的词补齐插入（约 125 词），level='ielts'，topic='ielts_vocabulary'，source='extracted'。
5. **表结构升级**：words 表新增 `list_no INTEGER`（可空，IELTS 1-24 按映射填）、`is_extra INTEGER DEFAULT 0`；Cambridge 真题来源的 282 词（source='Cambridge IELTS X Test Y'）设 is_extra=1、list_no 为 NULL，不参与 List 训练。
6. 写幂等迁移脚本（放 `server/db/`，可重复执行），并接入 `npm run setup`（migrate 后自动执行），确保从零建库也带 list_no。
7. 迁移后输出统计：每个 List 的词数、list_no 为 NULL 的词数、is_extra 数、IELTS/PET 总词数。

**验收标准**：
- `SELECT list_no, COUNT(*) FROM words WHERE is_extra=0 GROUP BY list_no` 与源文件每 List 词数一致（误差 ≤ 每 List 3 词）。
- IELTS 总词数 ≥ 2325，24 个 List 全部有词。
- Cambridge 282 词全部 is_extra=1 且 list_no 为 NULL。
- 随机抽查 20 个词，释义均有 2 个以上义项且准确。
- `npm run setup` 从零重建库后同样满足以上条件。

**不做**：不改动 PET 词（保持现状即可）；不删除 seed_extracted.js 里已有的词（只补不删）。

---

## P0-2：今日简报 + 每日训练流程重构

**任务**：把每日训练重构成「今日简报 → PET 热身 → 英译中（含错词死磕）→ 中译英 20% → 验收」的完整流程。

**背景**：现有 DailySetup → DailyFlashcards → DailyQuiz → DailyCorrection → DailyReport 流程是 v3.0 的雏形，需按 v4.0 方案重构。

**要做**：
1. **今日简报页**（进入 `/daily` 时展示）：
   - 今日目标时长（1 小时 + 欠债累积，上限 2 小时，计算规则见 P0-3 的 API）；
   - 原因说明（正常 / 昨日未训练 +30 / 昨日不足 45 分钟 +30 / 欠债 X 分钟）；
   - 今日内容预览：List N（下一个未完成 List，按 list_no 顺序）+ 待重背 List（若有）+ 抽查 List M（若到抽查日）+ PET 热身 10 词；
   - 按钮「现在开始」进入流程、「稍后再说」返回（可再次进入，简报状态不丢）。
2. **PET 热身**：10 个 PET 词（随机），英译中翻卡模式，显示"热身不计时不计分"标识，10 词过完自动进入主任务。
3. **英译中主任务**：取当前 List 全部词（list_no 最小的未完成 List），卡片模式看英文反应中文。按钮/快捷键标记"会/不会"。不会的进错词池。
   - 第一遍结束后自动进入**错词死磕循环**：只显示错词池，直到用户全部标记"会"，循环结束。
4. **中译英训练**：从当天 List 随机抽 20%（约 20 词，不足 20 词取全部），看中文释义拼英文，即时判定。
5. **验收**：中译英测验覆盖全部错词池单词（漏网之鱼），全部拼对才算完成当日任务。
6. 全程计时（PET 热身不计时），记录训练开始/结束时间。

**验收标准**：
- 按上述 5 步走完一次全流程，能看到每一步切换、错词循环、计时。
- 空状态（无 List 可学 / 24 个 List 全完成）有明确提示。
- 键盘快捷键延续既有约定（Enter 确认、Space 发音、←→ 翻卡、1/2 标记会/不会）。

**不做**：不做欠债计算逻辑（P0-3），不改写作模块。

---

## P0-3：时间盒与欠债系统（后端 API + 前端接入）

**任务**：实现训练计时与欠债规则的计算 API，前端简报与报告页接入。

**规则（必须精确实现）**：
- 每日目标时长 = 1 小时 + 累积欠债，单日上限 2 小时。
- 惩罚触发（次日 +30 分钟），满足任一：
  1. 当天完全无训练记录；
  2. 当天实际训练时长 < 当日目标时长 − 15 分钟。
- 欠债累积：惩罚叠加；练满 2 小时（达到上限）视为欠债全部结清，次日回到 1 小时。
- 当日完成训练任务（验收通过）即收工，即使未满 1 小时也不触发惩罚。

**要做**：
1. 新 API（建议 `server/routes/dailyPlan.js`）：
   - `GET /api/daily-plan`：返回 { targetMinutes, debtMinutes, reasonText, todayList, pendingReviewList, spotCheckList, petWarmupCount }；
   - `POST /api/daily-plan/complete`：标记当日训练完成（验收通过时调用），记录时长；
   - `GET /api/daily-plan/status`：查询今日是否已训练、已计时长、剩余建议时长。
2. 数据库：可扩展 `daily_sessions`（加 target_minutes, debt_minutes, completed 字段）或新建 `daily_debt` 表，规则计算基于每日记录。
3. 前端：今日简报页接入 `GET /api/daily-plan`；训练中显示已用时长/目标时长；收工时调用 complete。
4. 训练中若达 2 小时上限，弹提示"已达今日上限，欠债已结清"并允许收工。

**验收标准**：
- 构造测试场景验证：无训练记录 → 次日 target=90min 且 reasonText 正确；训练 30 分钟 → 次日 +30；连续欠债 3 天 → target=150min；练满 120min → 次日回到 60min；验收通过当日 → 次日无惩罚。
- 用 curl 或测试脚本给出上述 5 个场景的实际输出。

**不做**：不做定时提醒/通知（本地运行，无推送能力）。

---

## P1-4：抽查机制（间隔复习 v2）

**任务**：实现"背完 3-4 天后随机抽查已背 List 的 30 个词"机制，含正确率统计与待重背标记。

**要做**：
1. 新 API：
   - `GET /api/daily-plan` 中补充 spotCheck 逻辑：若存在"完成背诵已满 3-4 天且未抽查过"的 List，返回该抽查任务（随机选一个 List，随机抽 30 词，不足 30 取全部）；
   - `POST /api/spot-check`：提交抽查结果，计算正确率，≥80% 通过；否则该 List 标记待重背（写回 daily_sessions 或独立表）。
2. 前端：抽查界面（英译中翻卡 + 会/不会标记 + 结果页：正确率、通过/待重背提示）。
3. 待重背 List 在次日简报中优先展示（"今日优先重背 List M"），重背计入当日训练时间，不额外加时。
4. 抽查完的 List 记录抽查日期，避免重复抽查。

**验收标准**：
- 模拟一个 4 天前完成的 List，`GET /api/daily-plan` 返回抽查任务；提交 24/30 正确 → 通过；提交 20/30 → 标记待重背，次日简报显示。
- 待重背 List 不会再次被随机抽到（直到重背完成）。

---

## P1-5：中译英释义深度扩充（DeepSeek 批量优化）

**任务**：在 P0-1 基础释义之上，对全部 IELTS 词的中文释义做深度扩充（多义项、搭配提示），提升中译英训练质量。

**背景**：P0-1 已保证每词 2-4 义项；本任务做深度优化（如补充词性差异义项、常见搭配、易混提示）。

**要做**：
1. 写批量脚本 `server/scripts/enrichDefinitions.js`：
   - 读取 words 表 level='ielts' 且 is_extra=0 的词（约 2325 个），按 list_no 分批；
   - 调用 DeepSeek API（复用 server/.env 的 DEEPSEEK_API_KEY，注意限流与超时，每批 20-50 词，失败重试）；
   - 提示词要求：给出 2-4 个核心中文义项，用分号分隔，词性标注，单条 JSON 输出便于解析；
   - 写回 chinese_definition。
2. 幂等：已扩充的词可跳过；支持断点续跑。
3. 成本控制：默认一次跑 1-2 个 List 验证效果，确认后全量跑（或提供 --lists 参数按需跑）。

**验收标准**：
- 随机抽查 20 个词，释义均为 2 个以上义项且准确（人工抽验）。
- 脚本可断点续跑、可指定 List 范围。
- 中译英训练界面能显示扩充后的丰富释义。

**注意**：如果 DeepSeek API 调用成本敏感或超时频繁，可降级为本地规则（从既有 example_sentence 或常见搭配提取），但优先 DeepSeek。

---

## 执行顺序与验收清单

1. P0-1 → 跑 `npm run setup` + 统计脚本验证词库（每 List 词数、缺口补齐、释义抽验）
2. P0-2 → 手动走一遍完整训练流程
3. P0-3 → curl 验证 5 个欠债场景
4. P1-4 → 模拟抽查场景验证
5. P1-5 → 小批量验证效果后全量

每步完成更新 `CONVERSATION-HISTORY.md`（追加 v4.0 记录），全部完成后更新 README 与 TEST-REPORT。
