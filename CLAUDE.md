# IELTS 训练网站 — 项目记忆与需求文档

> 本文件是 Claude Code 的项目记忆（CLAUDE.md），每次会话自动加载。
> 技术细节与历史决策见 `CONVERSATION-HISTORY.md`，测试记录见 `TEST-REPORT.md`。
> 版本：v4.0 需求基线（2026-08-07）

---

## 一、项目定位

单人使用的雅思 6.5 分备考训练网站。React + Vite + Tailwind（前端）+ Express + SQLite（后端），本地运行。
两大模块：**单词背诵**（核心，本文档重点）+ **写作训练**（维持现状，本期不改）。

- 用户：本人（单用户，`user_id` 动态获取机制已存在）
- 版本管理：git（master 分支，历史提交 v2.0 → v3.2）
- 启动：`server` 端口 3001（npm run dev），`client` 端口 5174（npm run dev）

## 二、词库现状（整理任务由 Claude 执行，见 PROMPTS.md P0-1）

**源文件**：`C:\Users\15782\Downloads\雅思训练词库\`（项目目录外，读取被拦截时用 `--add-dir` 授权）

| 文件 | 结构（已核实） |
|------|----------------|
| 雅思 List 1-24 英译中.pdf | 24 List / 2329 词 / 全局编号 1-2329 / 单词+词性（释义列空） |
| 雅思 List 1-24 中译英.pdf | 24 List / 每 List 30 重点词 / 720 词 / 词性+中文释义（英文留空、顺序打乱） |
| PET 1-30 英译中.pdf | 25 List / ~2056 词 / 单词+词性 |
| PET 1-30 中译英.pdf | 25 List / ~2062 词 / 词性+中文释义 |
| 雅思真词汇英译中/中译英.docx | 旧版 24 表格默写卷，可作释义交叉参考 |

**数据库现状**：
- `seed_extracted.js` 已含 3955 词（IELTS 1941 + PET 2014），已导入 words 表。
- words 表**无 list_no 字段**；IELTS 词与源文件有约 384 词缺口（约 125 词完全缺失，如 abstract/academic/accelerate/artificial）。
- `server/db/ielts_list_map.json` 为辅助映射（word → {list_no, seq}，2325 唯一词），可直接使用或自行核对。

| 词库 | List 数 | 词数 | 说明 |
|------|---------|------|------|
| 雅思真词汇（IELTS） | 24 个 | 2329 词（PDF 全局编号 1-2329） | **训练主力**，List 1 最重要，List 内按字母序，每 List 约 100 词（List 5=118、List 12=59、List 24=72 等不均） |
| PET 核心词 | 25 个 | ~2056 词 | 热身用，每 List 60-76 词 |
| Cambridge 真题词 | — | 282 词 | 早期从真题提取，source 为 'Cambridge IELTS X Test Y'，**不参与 List 训练**，is_extra=1 |

## 三、训练方案（v4.0 核心需求）

### 3.1 每日流程（严格按此顺序）

1. **今日简报**（进入训练页时展示，必须实现）
   - 今日目标时长（默认 1 小时 + 欠债累积，上限 2 小时）
   - 为什么是这个时长（如"昨日未训练 +30 分钟 / 昨日不足 45 分钟 +30 分钟 / 正常"）
   - 今日预计内容：List N（新词）+ 抽查 List M（如有）+ PET 热身 10 词
   - 两个按钮：**[现在开始]** / **[稍后再说]**
2. **PET 热身**：10 个 PET 词英译中快速翻卡。**不计入训练时间、不计入正确率**，不穿插在正题中间。
3. **英译中主任务**：当天 List 的全部单词（约 100 词），看英文能反应出中文意思即算"过"。
   - 不会的词进入错词池。
   - **错词死磕**：第一遍过完后，错词池循环训练，直到全部正确为止（无限循环，如 100 词错 20 个 → 只练这 20 个 → 还有错的继续 → 直到全对）。
4. **中译英拼写训练**：权重为英译中的 20%（约 20 个词）。看中文释义拼写英文。
   - **中文释义必须足够丰富**（给出该词的核心义项，不局限于机构文档里的单一释义；可由 DeepSeek 生成扩充）。
5. **验收**：完成中译英测验（覆盖全部"漏网之鱼"错词），全部正确才算完成当天任务。

### 3.2 抽查机制（间隔复习）

- 每背 3-4 天后，从**已背诵过的 List** 中随机选一个，抽取其中 **30 个词**做抽查，检查正确率。
- 正确率 **≥ 80% 算过**；不达标则该 List 标记为"待重背"，次日优先重背该 List（重背计入当日训练时间，不额外加时）。

### 3.3 时间盒与欠债规则（核心逻辑）

- 每日目标：**1 小时**（全流程计时：英译中 + 拼写 + 抽查都算，PET 热身不算），或**完成当日任务**即可收工（两者取先到）。
- 若进度不乐观，用户可自行适当增加当日训练时间。
- **次日 +30 分钟惩罚**，触发条件（任一）：
  1. 当天完全没做任何训练；
  2. 训练时长 < 目标时长 − 15 分钟（如目标 1 小时，训练不足 45 分钟）。
- 欠债**累积**：每天 +30 分钟，**单日上限 2 小时**。练满 2 小时视为欠债全部结清，次日回到 1 小时基准。

### 3.4 训练记录

- 需要记录：每日训练开始/结束时间、实际训练时长、当日 List、英译中/中译英/抽查的正确率、错词列表、欠债状态、待重背 List。
- 现有 `daily_sessions` / `daily_session_words` 表可扩展，也可新建表，以清晰为准。

## 四、本期任务范围（给 Claude 的边界）

**要做**：
1. **词库整理**（P0-1）：解析 `C:\Users\15782\Downloads\雅思训练词库\` 的 4 个源 PDF，以英译中版为主表重建 IELTS 词库；释义按「中译英 PDF 匹配 → seed_extracted.js 现有释义 → DeepSeek 生成」三级补齐并扩充为 2-4 义项；补全约 125 个缺口词；words 表加 `list_no` + `is_extra` 字段（Cambridge 282 词 is_extra=1）。
2. 前端：新增"今日简报"；重构每日训练流程为「PET 热身 → 英译中（含错词死磕循环）→ 中译英 20% → 验收」；训练计时（全流程）。
3. 后端：训练会话、计时、欠债计算、抽查抽取、待重背 List 的 API。
4. 中译英释义深度扩充（DeepSeek 批量生成，注意成本与超时，可分批）。

**不做**（明确排除）：
- 写作模块任何改动（选题/编辑器/批改/历史全部维持现状）。
- 不重新提取词库（数据已在 seed_extracted.js）。
- 不动测试数据以外的数据迁移（数据库可保留，git 已做版本管理）。

## 五、技术约束与既有约定

- 键盘快捷键全覆盖是既有设计传统，新页面尽量延续（Enter/Space/←→/1-6/?/Esc 等）。
- API 风格：REST，`server/routes/*.js`，错误处理用 Toast + console.warn（勿用空 catch）。
- 前端：React Context + useReducer，页面组件在 `client/src/pages/`。
- 语音：只选 `localService=true` 的本地语音（Google 远程语音被墙，见 v3.1 修复），`utils/speech.js`。
- DeepSeek API Key 在 `server/.env`（DEEPSEEK_API_KEY），写作批改已接入；释义扩充可复用同一 Key 与调用封装。
- 交互反馈时长约定：正确/错误 800ms 左右，Toast 3s。
